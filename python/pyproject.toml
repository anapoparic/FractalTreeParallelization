[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "fractal-tree"
version = "0.1.0"

[tool.setuptools]
py-modules = ["sequential", "parallel", "sequential_asymmetric", "parallel_asymmetric", "utils"]